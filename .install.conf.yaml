- defaults:
    link:
      create: true
      relink: true

- clean: ["~", "~/.config"]

- link:
    ~/.shell:
    ~/.zsh:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.vim:
    ~/.vimrc:
    ~/.tmux.conf:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
