- defaults:
    link:
      create: true
      relink: true

- clean: ["~", "~/.config"]

- link:
    ~/.curlrc:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.vim:
    ~/.vimrc:
    ~/.tmux.conf:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
